# Pi Zero 2 W (64-bit) Configuration

# 1. Kernel (Must be 'Image' for 64-bit)
kernel=Image
arm_64bit=1

# 2. Serial Console (Critical for Zero 2 W)
dtoverlay=dwc2
dtoverlay=miniuart-bt

# 3. DRM/KMS with 3D Acceleration (required for display application)
dtoverlay=vc4-kms-v3d

# 4. Boot Files
start_file=start.elf
fixup_file=fixup.dat

# 5. Memory Split (64MB minimum for HW video decode + GLES2 rendering)
gpu_mem=64

# 6. Boot Speed
boot_delay=0
disable_splash=1